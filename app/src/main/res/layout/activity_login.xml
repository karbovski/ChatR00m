<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_vertical|center_horizontal"
    android:orientation="vertical"
    tools:context=".LoginActivity">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_marginBottom="30dp"
        android:adjustViewBounds="false"
        android:cropToPadding="false"
        android:onClick="logoOnClick"
        android:paddingBottom="30dp"
        app:srcCompat="@drawable/group" />

    <EditText
        android:id="@+id/serverIPText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="Server IP"
        android:inputType="textUri"
        android:text="192.168.43.147"
        android:textAlignment="center" />

    <EditText
        android:id="@+id/portText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="Port"
        android:inputType="number"
        android:text="61000"
        android:textAlignment="center" />

    <EditText
        android:id="@+id/usernameText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="Username"
        android:inputType="textPersonName"
        android:text="bobo"
        android:textAlignment="center" />

    <Button
        android:id="@+id/loginButtton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:onClick="joinChatOnClick"
        android:text="Connect to chat" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Messages are not encrypted, use on your own risk!"
        android:textAlignment="center"
        android:textStyle="bold" />

</LinearLayout>